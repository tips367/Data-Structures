// MinJumpToReachEnd.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>

int minimumJump(int arr[], int n)
{
    int* dp = new int[n];
    dp[0] = 0;
    if (n == 0 || arr[0] = 0)
        return -1;
    for (int i = 1; i < n; i++)
    {
        for (int j = 0; j < i; j++)
        {
            if()
        }
    }
}

int main()
{
    std::cout << "Hello World!\n";
}

